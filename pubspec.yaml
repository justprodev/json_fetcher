name: json_fetcher
description: Convenient fetching of JSON data, caching, auth, logging.
version: 2.0.3
repository: https://github.com/justprodev/json_fetcher

topics:
  - http
  - json
  - cache

environment:
  sdk: ^3.4.0

dependencies:
  http: ^1.2.1
  meta:
  web: ^1.1.0

  logging:

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.9
  http_parser:

  mock_web_server:
    git:
      url: https://github.com/justprodev/MockWebServer.git
      ref: 5.0.0-nullsafety.1